<?php
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
    exit();
}

/**
 * Financial Calculator Suite Uninstall
 * 
 * Currently no options or custom tables to delete.
 * In future, if plugin stores options in DB, delete them here.
 */

// Example: Delete plugin options if added in future
// delete_option('fcs_some_option');
